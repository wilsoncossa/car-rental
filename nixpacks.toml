# Runtime packages
packages = ["nodejs_22", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

# Run migrations on start, then boot the app
start = "npm run db:push && npm start"
