[phases.setup]
packages = ["nodejs_22", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  # garante que o schema do Postgres exista antes de seed/start
  "npm run db:push",
  "npm run build"
]

[start]
cmd = "npm start"
