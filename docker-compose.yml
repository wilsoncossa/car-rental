services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    # Add volumes if needed, e.g. to persist uploads:
    # volumes:
    #   - ./uploads:/app/uploads
